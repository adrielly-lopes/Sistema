CREATE DATABASE `fmap`;
USE `fmap`;

CREATE TABLE `usuario` (
    `id` int(10) unsigned NOT NULL AUTO_INCREMENT,
    `Nome` varchar(200) NOT NULL,
    `Email` varchar(250) NOT NULL UNIQUE,
    `Senha` varchar(100) NOT NULL,
    `Nivel` varchar(50) NOT NULL,
    PRIMARY KEY (`id`)
);
