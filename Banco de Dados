CREATE DATABASE `sistema fmap`;
USE `sistema fmap`;

CREATE TABLE `usuarios` (
    `id` int(10) unsigned NOT NULL AUTO_INCREMENT,
    `Login` varchar(200) NOT NULL UNIQUE,
    `Senha` varchar(100) NOT NULL,
    PRIMARY KEY (`id`)
);
